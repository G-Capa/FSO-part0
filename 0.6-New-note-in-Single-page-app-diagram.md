# 0.6- New-note-on-Single-page-app-diagram

Exercise number 0.6 from Full Stack Open Course by University of Helsinki

Diagram depicting the situation where the user creates a new note  on the single-page app version of the notes app at https://studies.cs.helsinki.fi/exampleapp/spa.

```mermaid
graph TD
E{**BROWSER**} ===> A
A["User writes a note and clicks **Save**"] 
A-.-> C["Form submission intercepted by JS \(e.preventDefault()\)"]
C--> D["New note object created with content + timestamp"]
D--> F["Notes array updated and page re-rendered with redrawNotes()"]
F--> G["JS sends POST to server at new_note_spa"]
G--> B{**SERVER**}
B--> H["Responds with 201 Created"]
H-.-> F

E --> I["Initial GET request for notes.json"]
I --> F

classDef green fill:#d1fae5,color:#065f46
classDef blue fill:#dbeafe,color:#1e3a8a
classDef yellow fill:#fff7ed,color:#b45309
classDef red fill:#fee2e2,color:#b91c1c

class B green
class G blue
class C yellow
class D yellow
class F yellow

